import { DuracionHorasMinutosPipe } from './duracion-horas-minutos.pipe';

describe('DuracionHorasMinutosPipe', () => {
  it('create an instance', () => {
    const pipe = new DuracionHorasMinutosPipe();
    expect(pipe).toBeTruthy();
  });
});
