import { FailedImageDirective } from './failed-image.directive';

describe('FailedImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FailedImageDirective();
    expect(directive).toBeTruthy();
  });
});
